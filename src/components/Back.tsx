import React from "react";
import { back } from "../assets/icons";

const Back = () => {
  return (
    <div className="h-36 flex items-center px-[10vw]">
      <img src={back} alt="" />
    </div>
  );
};

export default Back;
